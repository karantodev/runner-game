# Asset Duplicate Cleanup Proposal

Generated 2026-05-30T20:57:52.082Z.

Per SHA-256 group: one canonical path is selected by the canonical-rank
heuristic (`pickups/<feature>/` > `effects/<name>/` > `ui/` > `structures/`).
Non-canonical copies are proposed for archive at
`assets/_source/rejected_YYYY_MM_DD/duplicates/<group-id>/`.

Total duplicate groups: **1**

## Group #1 — 1254×1254 (2 copies)

- **Keep (canonical)**: `assets/structures/question_block/question_block_02.png`
- **Archive**:
  - `assets/structures/question_block/question_block_04.png`

## Action
After approval, move archive entries to `_source/rejected_YYYY_MM_DD/duplicates/` —
do NOT delete. Designer may want to recover.